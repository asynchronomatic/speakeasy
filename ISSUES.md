# Current Known Issues

* When a state change in the mesh occurs on a leaf node, our UI does not get a refresh ( we only refresh if peer node impacts us ). We need to propagate the leaf node change to all other nodes.
* Stats updates require a manual refresh
* Document how to pin ollama models
* Add ability to refresh provider models ( should be done async )


## FAQ


* Document security concerns?
** What am i actually letting ono my network?
** Encryption Story
** Link to libp2p Security Page ?
  
* OpenSense documentation. ( Open sense breaks hole punch, document workarounds )
* Document how to effectively pin OLLAMA models to prevent model thrashing

 